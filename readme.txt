
People are struggling to read bus names, street names, shop names which
are written in unknown languages.  To overcome this problem this Language
Reader Application captures the images such as bus board, shop name etc. From the captured image, it extracts the text and transliterates it in the language that the user wants. This application can also read the transliterated text in user readable language.